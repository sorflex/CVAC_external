Each package is designed to expand from the root ...CVAC/ext/ and fills in 
the given directory structure below (hinted in the filename).

The one (.zip) that is different is CVAC_ext_mainPkg.zip, which contains
the various foldes of include files that were stored in /ext.

